# Correct format for MongoDB Atlas connection strings
# Make sure to replace <password> with your actual password (and encode special characters if needed).
# Also, replace myFirstDatabase with the actual database name you want to use (e.g., "pm-ai-agent").
MONGODB_URI=mongodb+srv://your_username:<password>@your_cluster_name.mongodb.net/myFirstDatabase?retryWrites=true&w=majority

# API Keys
GROQ_API_KEY=gsk_your_groq_key_here